# docker-php-apache-mysql

Для запуска использовать:
```bash
docker-compose up
```

Для запуска и  работы в фоне:
```bash
docker-compose up -d
```

Остановить работу:
```bash
docker-compose stop
```